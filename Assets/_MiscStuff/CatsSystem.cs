using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Collect as many cats as you can find (Leveling substitute)
public class CatsSystem : MonoBehaviour
{
    [SerializeField] int catsFound;
    [SerializeField] int maxcats = 200;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void RelationshipMultiplier()
    {
        
    }
}
